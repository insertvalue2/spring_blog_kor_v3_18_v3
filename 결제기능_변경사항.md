# 결제 기능(포트원 V2 PG 연동) 변경사항 정리

> 포인트 충전을 실제 카드/카카오페이 결제로 처리하는 기능입니다.
> 처음에는 포트원 V1(아임포트, iamport.js)으로 만들었으나, 결제 검증 단계에서 계속 실패하여 포트원 V2 규격으로 전면 전환했습니다.
> 이 문서는 V2 기준 최종 코드와, V1에서 V2로 바꾸면서 달라진 점을 함께 정리합니다.

---

## 0. 왜 V1에서 V2로 바꿨나 (이번 전환의 배경)

V1으로 구현했을 때, 카카오페이 결제는 "완료"로 떴는데 서버에서 결제를 조회하면 "존재하지 않는 결제정보입니다"(404)가 반복됐습니다.

원인은 결제가 만들어진 환경과 조회하는 API 버전이 어긋난 것이었습니다. 포트원은 현재 V1을 레거시로 두고 신규 연동은 V2를 권장하며, 최근 콘솔에서 만든 계정/채널은 V2 기반인 경우가 많습니다. V1 결제 SDK로 결제는 진행됐지만, V1 조회 API(api.iamport.kr)에는 해당 결제가 없어서 404가 났던 것입니다.

그래서 결제 SDK와 서버 검증을 모두 V2로 통일했습니다. 결과적으로 서버 코드는 오히려 단순해졌습니다(토큰 발급 단계가 사라짐).

비유하면, V1과 V2는 같은 건물의 서로 다른 출입 시스템입니다. 카드(결제)는 V2 문으로 들어왔는데, 경비실(서버)은 V1 출입기록만 뒤지고 있었던 셈입니다. 둘 다 V2로 맞추니 기록이 일치하게 됐습니다.

---

## 1. 결제 전체 흐름 (V2)

결제는 3단계로 나뉩니다. 한 번의 요청으로 끝나지 않는다는 점은 V1과 동일합니다.

```
[1] prepare (결제 건 생성)
    프론트 → 우리 서버
    "5000원짜리 충전 결제 건 만들어줘"
    우리 서버 → 프론트
    "결제 건 식별자(paymentId) point_1_1716... + storeId + channelKey 줄게"

[2] 포트원 V2 결제창 (실제 결제)
    프론트 → PortOne.requestPayment(...)
    카카오페이(간편결제) 결제 진행
    포트원 → 프론트
    "결제 성공, paymentId 그대로 돌려줄게"

[3] complete (검증 + 포인트 지급)
    프론트 → 우리 서버
    "이 paymentId 확인하고 포인트 줘"
    우리 서버 → 포트원 서버 (직접 조회)
    GET https://api.portone.io/payments/{paymentId}
    포트원 → 우리 서버
    "status=PAID, amount.total=5000"
    우리 서버: 검증 통과 → 포인트 5000 충전 → 결제내역 저장
```

[그림설명 필요] 프론트 / 우리 서버 / 포트원 서버 3개 박스에 prepare → 결제창 → complete 화살표가 오가는 시퀀스 다이어그램

### 왜 서버가 또 검증하나 (보안 핵심 — V1과 동일한 원칙)

프론트(브라우저)가 보내는 정보는 사용자가 조작할 수 있습니다. 그래서 결제창의 성공 응답만 믿지 않고, 서버가 포트원에 직접 조회해서 진짜 결제됐는지, 금액이 얼마인지를 확인한 뒤에만 포인트를 줍니다.

비유: 결제창 성공 응답은 "손님이 직접 써온 영수증"입니다. 가게(서버)는 그 종이를 믿지 않고 카드사(포트원)에 전화해 거래를 확인합니다. 이 확인이 complete 단계입니다.

---

## 2. 신규 / 변경 파일 목록

### payment 패키지 (6개) — V2 규격으로 재작성

| 파일 | 역할 |
|------|------|
| payment/Payment.java | 결제 내역 엔티티. 식별자가 paymentId(+pgTxId)로 변경 |
| payment/PaymentRepository.java | findByPaymentId / existsByPaymentId (중복 지급 방지) |
| payment/PaymentRequest.java | PrepareDTO(amount) / CompleteDTO(paymentId) |
| payment/PaymentResponse.java | PrepareDTO(paymentId, amount, storeId, channelKey) / CompleteDTO / PortOnePaymentV2(조회 응답 매핑) |
| payment/PaymentService.java | paymentId 발급 + V2 단건 조회로 검증 + 포인트 충전 |
| payment/PaymentController.java | POST /api/payment/prepare, POST /api/payment/complete |

### 화면

| 파일 | 역할 |
|------|------|
| templates/user/charge-point.mustache | 포트원 V2 SDK(browser-sdk)로 결제창 호출 |

### 설정 / 진입점

| 파일 | 변경 내용 |
|------|-----------|
| user/UserController.java | "/user/charge-point" GET 매핑 (변경 없음, 그대로 사용) |
| templates/user/detail.mustache | "PG 결제로 충전하기" 버튼 (변경 없음, 그대로 사용) |
| application.yml | portone.store-id / channel-key / api-secret 로 교체 |
| .env | PORTONE_STORE_ID / PORTONE_CHANNEL_KEY / PORTONE_API_SECRET 추가 |

---

## 3. V1 대비 핵심 차이 (가장 중요한 비교표)

| 항목 | V1 (아임포트) | V2 (포트원) |
|------|---------------|-------------|
| 프론트 SDK | cdn.iamport.kr/v1/iamport.js, IMP.request_pay(콜백) | cdn.portone.io/v2/browser-sdk.js, PortOne.requestPayment(async) |
| 결제창 초기화 키 | 가맹점 식별코드(impKey) | storeId + channelKey |
| 결제 건 식별자 | merchant_uid(우리) + imp_uid(포트원) | paymentId(우리가 발급, 단일) |
| 서버 인증 | imp_key/imp_secret로 토큰 발급(2단계) | API Secret을 헤더에 직접 사용(토큰 단계 없음) |
| 인증 헤더 | Authorization: Bearer {토큰} | Authorization: PortOne {API_SECRET} |
| 결제 조회 | GET api.iamport.kr/payments/{imp_uid} | GET api.portone.io/payments/{paymentId} |
| 응답 래핑 | { code, message, response } 안에 들어있음 | 결제 객체가 최상위에 평평하게 옴 |
| 응답 표기 | snake_case (@JsonNaming 필요) | camelCase (별도 네이밍 전략 불필요) |
| 성공 상태값 | "paid" | "PAID" |
| 결제 금액 | response.amount | amount.total |
| 카카오페이 지정 | pg: 'kakaopay' | payMethod: 'EASY_PAY' (+ 카카오페이 채널의 channelKey) |
| 결제 엔드포인트 | /api/payment/verify | /api/payment/complete |

핵심은 "토큰 발급 단계가 사라졌다"는 점입니다. V1은 토큰을 받고(getToken) 그 토큰으로 결제를 조회하는 2단계였는데, V2는 API Secret을 헤더에 바로 실어 한 번에 조회합니다.

---

## 4. 핵심 코드 설명

### 4-1. PaymentController — 두 개의 엔드포인트

```java
@RestController
public class PaymentController {

    private final PaymentService paymentService;

    // [Step 1] 결제 건 생성: paymentId + storeId + channelKey 반환
    @PostMapping("/api/payment/prepare")
    public ResponseEntity<?> preparePayment(@RequestBody PaymentRequest.PrepareDTO reqDTO,
                                            HttpSession session) {
        reqDTO.validate();
        User sessionUser = (User) session.getAttribute("sessionUser");
        if (sessionUser == null) {
            return ResponseEntity.status(401).body(Map.of("message", "로그인이 필요합니다."));
        }
        PaymentResponse.PrepareDTO prepareDTO =
                paymentService.결제요청생성(sessionUser.getId(), reqDTO.getAmount());
        return ResponseEntity.ok().body(Map.of(
                "paymentId", prepareDTO.getPaymentId(),
                "amount", prepareDTO.getAmount(),
                "storeId", prepareDTO.getStoreId(),
                "channelKey", prepareDTO.getChannelKey()
        ));
    }

    // [Step 3] 검증 + 포인트 충전
    @PostMapping("/api/payment/complete")
    public ResponseEntity<?> completePayment(@RequestBody PaymentRequest.CompleteDTO reqDTO,
                                             HttpSession session) {
        reqDTO.validate();
        User sessionUser = (User) session.getAttribute("sessionUser");
        if (sessionUser == null) {
            return ResponseEntity.status(401).body(Map.of("message", "로그인이 필요합니다."));
        }
        PaymentResponse.CompleteDTO completeDTO =
                paymentService.결제검증및충전(sessionUser.getId(), reqDTO.getPaymentId());

        // 세션 포인트 즉시 갱신 (충전 반영)
        sessionUser.setPoint(completeDTO.getCurrentPoint());
        session.setAttribute("sessionUser", sessionUser);
        return ResponseEntity.ok().body(completeDTO);
    }
}
```

V1과 동일하게 두 가지를 주의합니다.

첫째, "/api/**"는 이 프로젝트의 LoginInterceptor 보호 대상이 아니므로, 컨트롤러에서 직접 sessionUser null 검사로 로그인 여부를 확인합니다.

둘째, complete 성공 후 세션의 포인트를 다시 넣어줍니다. DB만 갱신하면 세션의 sessionUser는 옛 잔액이라 화면에 충전이 반영되지 않습니다.

### 4-2. PaymentService — prepare (결제 건 생성)

```java
@Transactional
public PaymentResponse.PrepareDTO 결제요청생성(Integer userId, Integer amount) {
    if (!userRepository.existsById(userId)) {
        throw new Exception404("사용자를 찾을 수 없습니다");
    }
    String paymentId = generatePaymentId(userId);
    while (paymentRepository.existsByPaymentId(paymentId)) {
        paymentId = generatePaymentId(userId);
    }
    return new PaymentResponse.PrepareDTO(paymentId, amount, storeId, channelKey);
}

private String generatePaymentId(Integer userId) {
    return "point_" + userId + "_" + System.currentTimeMillis()
            + "_" + UUID.randomUUID().toString().substring(0, 8);
}
```

paymentId는 결제의 신분증입니다. 프론트가 정하면 조작/중복 결제가 가능하므로, 서버가 시간값과 랜덤값을 섞어 유일하게 발급합니다. storeId와 channelKey는 프론트 결제창 호출에 필요한 공개값이라 함께 내려줍니다.

### 4-3. PaymentService — complete (검증 + 충전, 가장 중요)

```java
@Transactional
public PaymentResponse.CompleteDTO 결제검증및충전(Integer userId, String paymentId) {
    User user = userRepository.findById(userId)
            .orElseThrow(() -> new Exception404("사용자를 찾을 수 없습니다"));

    // 같은 paymentId 로 이미 지급했으면 중단 (중복 지급 방지)
    if (paymentRepository.findByPaymentId(paymentId).isPresent()) {
        throw new Exception400("이미 처리된 결제입니다");
    }

    // 포트원 V2 서버에 직접 조회
    PaymentResponse.PortOnePaymentV2 payment = 포트원결제조회(paymentId);

    // 무결성 검증: 상태가 PAID 인지, 금액 정보가 있는지
    if (payment.getStatus() == null || !"PAID".equals(payment.getStatus())) {
        throw new Exception400("결제가 완료되지 않았습니다. (상태: " + payment.getStatus() + ")");
    }
    if (payment.getAmount() == null || payment.getAmount().getTotal() == null) {
        throw new Exception400("결제 금액 정보를 확인할 수 없습니다.");
    }

    // 금액은 프론트가 아니라 포트원이 알려준 실제 결제 금액 사용
    Integer amount = payment.getAmount().getTotal();
    user.chargePoint(amount);              // 더티 체킹으로 자동 UPDATE

    Payment record = Payment.builder()
            .paymentId(paymentId)
            .pgTxId(payment.getPgTxId())
            .user(user).amount(amount).status("PAID")
            .build();
    paymentRepository.save(record);

    return new PaymentResponse.CompleteDTO(amount, user.getPoint());
}
```

3중 안전장치는 V1과 같은 철학입니다.

첫째, 중복 지급 방지. 같은 paymentId가 이미 DB에 있으면 거부합니다. complete가 두 번 호출돼도 포인트가 두 번 들어가지 않습니다(paymentId 컬럼이 unique인 것도 같은 방어).

둘째, 금액은 포트원이 알려준 amount.total을 씁니다. 프론트가 금액을 위조해도 실제 결제액만 적립됩니다.

셋째, 결제 내역(Payment) 저장은 영수증입니다. 추후 추적/정산에 사용합니다.

### 4-4. 포트원 V2 결제 조회 (토큰 단계 없음)

```java
private PaymentResponse.PortOnePaymentV2 포트원결제조회(String paymentId) {
    try {
        HttpHeaders headers = new HttpHeaders();
        // V2 인증: Authorization 헤더에 "PortOne {API_SECRET}" 직접 전달
        headers.set("Authorization", "PortOne " + apiSecret);
        HttpEntity<Void> request = new HttpEntity<>(headers);

        RestTemplate restTemplate = new RestTemplate();
        ResponseEntity<PaymentResponse.PortOnePaymentV2> response = restTemplate.exchange(
                "https://api.portone.io/payments/" + paymentId,
                HttpMethod.GET, request,
                PaymentResponse.PortOnePaymentV2.class);

        PaymentResponse.PortOnePaymentV2 data = response.getBody();
        if (data == null) throw new Exception400("결제 정보를 찾을 수 없습니다.");
        return data;
    } catch (Exception400 e) {
        throw e;
    } catch (Exception e) {
        log.error("포트원 결제 조회 실패: {}", e.getMessage());
        throw new Exception400("결제 검증 중 오류가 발생했습니다: " + e.getMessage());
    }
}
```

V2는 토큰을 따로 받지 않습니다. API Secret을 헤더에 바로 실어 한 번의 GET으로 조회합니다. (토큰 방식을 쓰려면 "POST /login/api-secret"으로 토큰을 발급받아 "Bearer {토큰}"으로 보낼 수도 있으나, 본 강의는 단순한 직접 인증 방식을 사용합니다.)

### 4-5. V2 응답 매핑 (@JsonIgnoreProperties)

V2 응답은 camelCase라 V1처럼 snake_case 변환 전략(@JsonNaming)이 필요 없습니다. 대신 결제 객체에는 필드가 매우 많으므로, 필요한 필드만 매핑하고 나머지는 무시합니다.

```java
@Data
@JsonIgnoreProperties(ignoreUnknown = true)
public static class PortOnePaymentV2 {
    private String status;   // "PAID"
    private String id;       // paymentId
    private String pgTxId;   // PG 거래 번호
    private Amount amount;

    @Data
    @JsonIgnoreProperties(ignoreUnknown = true)
    public static class Amount {
        private Integer total;   // 총 결제 금액 (충전 포인트)
        private Integer taxFree;
        private Integer vat;
    }
}
```

### 4-6. charge-point.mustache — V2 결제창 호출

```html
<script src="https://cdn.portone.io/v2/browser-sdk.js"></script>
<script>
  // [1] prepare → [2] 결제창 → [3] complete
  const prepare = await (await fetch('/api/payment/prepare', {
      method: 'POST', headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({ amount: selectedAmount })
  })).json();

  const payment = await PortOne.requestPayment({
      storeId: prepare.storeId,
      channelKey: prepare.channelKey,
      paymentId: prepare.paymentId,
      orderName: '포인트 충전',
      totalAmount: prepare.amount,
      currency: 'CURRENCY_KRW',
      payMethod: 'EASY_PAY',           // 카카오페이 = 간편결제
      customer: { email: '{{user.email}}', fullName: '{{user.username}}' }
  });

  if (payment.code !== undefined && payment.code !== null) {
      alert('결제에 실패하였습니다.\n' + payment.message);  // 실패/취소 시 code 존재
      return;
  }

  const result = await (await fetch('/api/payment/complete', {
      method: 'POST', headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({ paymentId: payment.paymentId })
  })).json();
</script>
```

V1과 가장 크게 다른 점은 두 가지입니다. 콜백 함수 방식(IMP.request_pay의 두 번째 인자)이 사라지고 async/await로 결과를 받습니다. 그리고 성공/실패 판별이 rsp.success가 아니라 응답의 code 존재 여부입니다(실패/취소 시에만 code가 들어옴).

카카오페이는 간편결제이므로 payMethod를 "EASY_PAY"로 보내고, 어떤 PG(카카오페이)로 갈지는 채널 키(channelKey)가 결정합니다.

### 4-7. 설계 노트 — 왜 서버 prepare 단계를 두는가 (포트원 문서 퀵가이드와의 차이)

포트원 공식 문서의 퀵 가이드는 paymentId를 프론트에서 직접 만듭니다.

```javascript
paymentId: `payment-${crypto.randomUUID()}`,   // 브라우저가 즉석 생성, prepare 단계 없음
```

이 프로젝트는 대신 서버에 먼저 요청(prepare)해서 paymentId(+ storeId, channelKey)를 받아옵니다. 즉 "prepare 단계가 없는 최소 버전"(문서)과 "서버 prepare 단계를 추가한 버전"(이 프로젝트)의 차이입니다.

서버에서 발급하는 이유는 네 가지입니다.

첫째, 식별자의 유일성을 서버가 보장합니다. 발급하면서 DB에 같은 paymentId가 있는지(existsByPaymentId) 확인하고 중복이면 재생성합니다. 우리 시스템이 그 번호를 책임지고 관리한다는 통제권이 서버에 있습니다.

둘째, storeId와 channelKey를 서버 설정(.env)에서 내려줍니다. 문서 방식은 이 값을 화면 자바스크립트에 직접 박아야 해서 계정 값이 여러 화면에 흩어집니다. prepare가 내려주면 .env 한 곳에서 관리되고, 채널이 바뀌어도 화면을 안 고쳐도 됩니다. (이전 V1의 계정 불일치 404를 구조적으로 예방하는 효과)

셋째, 결제 시작을 로그인 세션에 결속합니다. prepare 엔드포인트가 sessionUser를 확인하므로, 로그인한 사용자 명의로만 결제 건이 시작됩니다.

넷째, 확장성입니다. 서버가 결제 건을 발급하는 시점에 "이 사용자가 이 금액을 결제할 예정"이라는 주문 의도를 미리 기록해둘 수 있습니다. 그러면 complete 단계에서 실제 결제 금액과 사전 기록 금액을 대조해 금액 위변조를 한 번 더 막을 수 있습니다. 포트원도 이 목적의 "결제 정보 사전 등록(pre-register)" API를 제공합니다.

정직하게 짚어둘 점 두 가지가 있습니다.

문서의 퀵 가이드도 안전합니다. 두 방식 모두 진짜 보안 게이트는 마지막 서버 검증(complete에서 포트원 재조회)이기 때문입니다. prepare가 없어도 검증만 제대로 하면 금액·상태 위조는 막힙니다. 문서 방식은 틀린 게 아니라 가장 단순한 시작점입니다.

그리고 현재 이 프로젝트의 prepare는 paymentId만 발급하고 금액을 DB에 미리 저장하진 않습니다. 즉 위 넷째의 "금액 사전 대조"는 아직 코드로 구현된 것이 아니라, 그렇게 확장하기 좋은 구조까지만 갖춘 상태입니다(현재는 complete에서 포트원이 알려준 amount.total을 신뢰해 충전). 더 엄격히 하려면 prepare에서 (userId, 금액)을 기록하고 complete에서 대조하는 단계를 추가하면 됩니다. (확인 필요: 강의 범위에 따라 추가 여부 결정)

비유하면, 프론트 생성 방식은 손님이 주문번호를 직접 적어 와서 결제하는 것이고, 서버 prepare 방식은 가게(서버)가 주문번호를 발급하며 장부에 먼저 적어두는 것입니다. 둘 다 마지막에 카드사(포트원)에 거래를 확인하지만, 장부에 미리 적어두면 "이 번호로 얼마가 결제될 예정이었는지"까지 대조할 수 있어 더 촘촘합니다.

[그림설명 필요] 좌측은 프론트 생성(브라우저 → 포트원 바로), 우측은 서버 prepare(브라우저 → 우리 서버 → 포트원)로 두 흐름을 나란히 비교하는 그림

---

## 5. 반드시 해야 하는 설정 (이거 안 하면 결제 안 됨)

### 5-1. application.yml (이미 반영됨)

```yaml
portone:
  store-id: ${PORTONE_STORE_ID:}
  channel-key: ${PORTONE_CHANNEL_KEY:}
  api-secret: ${PORTONE_API_SECRET:}
```

":" 뒤를 비워둔 것은 값이 없어도 앱이 기동되게 하기 위함입니다. 값이 없으면 앱은 켜지지만 실제 결제 검증에서 실패합니다.

### 5-2. 포트원 콘솔에서 채널 먼저 만들기 (가장 중요)

V2는 채널 단위로 PG가 결정됩니다. 콘솔의 연동 정보 > 채널 관리에서 카카오페이 채널(테스트)을 먼저 추가하세요. 이 채널이 없으면 결제창 호출 단계에서 막힙니다. (이전 V1 404의 교훈: 결제가 어느 환경에서 만들어지는지가 채널에 달려 있습니다.)

### 5-3. .env 에 V2 값 입력 (사용자가 직접 해야 함)

세 값은 모두 포트원 콘솔의 같은 계정/같은 V2 연동 정보에서 가져옵니다.

```
PORTONE_STORE_ID=store-xxxxxxxx
PORTONE_CHANNEL_KEY=channel-key-xxxxxxxx
PORTONE_API_SECRET=(V2 API Secret)
```

콘솔에서 받는 위치는 다음과 같습니다.

| 값 | 위치 | 성격 |
|----|------|------|
| store-id (상점 ID) | 연동 정보 화면 상단 | 공개(프론트에서 사용) |
| channel-key (채널 키) | 연동 정보 > 채널 관리 > 해당 카카오페이 채널 | 공개(프론트에서 사용) |
| API Secret | 연동 정보 > 식별코드·API Keys > V2 API 탭 | 비밀(서버 전용, 절대 노출 금지) |

주의: V2 API Secret은 V1의 REST API Secret과 다른 값입니다. 콘솔 화면에서 "V1 API" 탭이 아니라 "V2 API" 탭의 Secret을 사용해야 합니다.

V2 API Secret은 서버 비밀키이므로 절대 깃에 커밋하면 안 됩니다(.env는 .gitignore에 등록되어 있어야 합니다). 노출되면 콘솔에서 즉시 재발급하세요.

---

## 6. 테스트 시나리오

1. 콘솔 연동 정보 > 채널 관리에서 카카오페이 테스트 채널 추가
2. .env에 PORTONE_STORE_ID, PORTONE_CHANNEL_KEY, PORTONE_API_SECRET(V2) 입력
3. 앱 완전 재시작 (아래 7번 주의 참고)
4. 로그인 → 마이페이지(/user/detail) → "PG 결제로 충전하기" 클릭
5. 충전 금액 선택 → "충전하기" → 카카오페이 결제창 진행(테스트 모드)
6. 결제 완료 → "충전이 완료되었습니다" alert → 마이페이지 잔액 증가 확인
7. payment_tb 테이블에 결제 내역 1행(status=PAID, paymentId, pgTxId)이 쌓였는지 DB 확인

---

## 7. 자주 겪는 오류 / 주의사항

앱 완전 재시작 필요. application.yml과 .env를 바꾼 뒤에는 새로고침이 아니라 애플리케이션을 완전히 종료 후 다시 실행해야 설정이 반영됩니다.

값 미설정 시 동작. 세 값을 안 넣어도 앱은 켜집니다(빈 기본값). 하지만 complete 단계에서 "결제 검증 중 오류가 발생했습니다"가 뜹니다. 이때는 store-id / channel-key / api-secret 설정을 다시 확인하세요.

채널 누락 주의. 카카오페이 채널이 없으면 결제창 호출(requestPayment) 자체가 실패합니다. 채널을 먼저 만들어야 합니다.

V1/V2 값 혼동 주의. 콘솔에 V1 API와 V2 API 탭이 따로 있습니다. 이번 코드는 V2이므로 V2 API Secret을 써야 합니다. V1 값을 넣으면 인증/조회가 어긋나 404 또는 인증 오류가 납니다(이전에 겪었던 그 증상입니다).

ddl-auto: create 주의. 이 프로젝트는 기동 시 테이블을 새로 만듭니다(create). 재시작할 때마다 payment_tb가 초기화됩니다. 결제 내역을 보존하려면 운영 단계에서는 update 또는 validate로 바꿔야 합니다. (확인 필요: 강의 단계 정책에 따라 다르므로 수강생에게 명시할 것)

세션 잔액이 안 바뀔 때. complete 후 세션 갱신 코드(session.setAttribute)가 있는지 확인하세요.

"/api/payment/**" 401. 로그인 안 한 상태로 결제하면 컨트롤러가 401을 반환합니다. 이 경로는 인터셉터 보호 대상이 아니므로 컨트롤러의 직접 null 검사가 유일한 방어선입니다.

---

## 8. 핵심 정리 (한 줄 요약)

결제는 prepare(서버가 paymentId 발급) → 결제창(포트원 V2 SDK) → complete(서버가 api.portone.io에 직접 재조회) 의 3단계이며, 포인트는 오직 status가 PAID인 실제 결제 금액(amount.total)으로만, 중복 지급을 막은 상태에서 지급된다. V2는 토큰 발급 단계가 없어 V1보다 서버 흐름이 단순하다.
