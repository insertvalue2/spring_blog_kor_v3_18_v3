# 결제 API 만들기 전 핵심 사전 지식 (포트원 V2 기준)

> 이 문서는 PG 결제 API를 직접 만들기 전에 알아야 할 개념을 간략히 정리한 것입니다.
> 코드보다 "왜 이렇게 설계하는가"를 먼저 이해하는 것이 목표입니다.

## 1. PG와 결제 대행이란 (왜 카드사에 직접 안 붙나)

PG사(결제 대행사)는 우리 서비스와 카드사·은행·간편결제 사이에서 결제를 중계해 주는 사업자입니다. 카드사마다 연동 규격이 달라 직접 붙으면 너무 복잡하기 때문에, 표준화된 창구 하나로 묶어주는 중계자를 둡니다.

포트원(구 아임포트)은 그 PG사들조차 하나의 규격으로 다시 묶어주는 상위 중계 서비스입니다. 우리 코드는 포트원 한 곳만 연동하면, 카카오페이·카드 등 결제 수단은 설정만 바꿔 교체할 수 있습니다.

비유: 포트원은 여러 은행과 연결된 공용 ATM망과 같습니다. 은행마다 다른 창구를 찾아갈 필요 없이, 공용망 한 곳에 붙으면 어느 은행이든 거래할 수 있습니다.

[그림설명 필요] 우리 서비스에서 포트원을 거쳐 여러 PG사·카드사로 이어지는 중계 구조도

## 2. 결제는 왜 한 번에 안 끝나고 여러 단계인가

결제는 보통 세 단계로 나뉩니다.

```
1단계 사전 준비 : 서버가 주문 식별자(주문번호)를 발급
2단계 결제창    : 사용자가 카카오페이/카드로 실제 결제
3단계 검증      : 서버가 결제가 진짜인지 확인한 뒤 처리(포인트 지급 등)
```

한 번의 요청으로 끝나지 않는 이유는, 실제 결제는 외부(PG)에서 일어나고 우리 서버는 그 결과를 나중에 다시 확인해야 하기 때문입니다.

비유: 식당에서 예약번호를 받고(1단계), 식사를 하고(2단계), 마지막에 영수증을 대조해 정산하는(3단계) 흐름과 같습니다.

[그림설명 필요] 프론트엔드 / 우리 서버 / 포트원 서버 세 박스 사이로 1단계, 2단계, 3단계 화살표가 오가는 시퀀스 다이어그램

## 3. 신뢰 경계: 브라우저(프론트)는 믿으면 안 된다

브라우저에서 서버로 오는 값은 사용자가 개발자 도구 등으로 얼마든지 조작할 수 있습니다. 그래서 "결제 성공했고 5만원 냈어요"라는 프론트의 말을 그대로 믿으면 안 됩니다. 최종 판단은 서버가 PG에 직접 물어봐서 내려야 합니다.

비유: 손님이 직접 써온 영수증을 믿지 않고, 가게가 카드사에 전화해 "이 거래 진짜 있었나요?"를 확인하는 것과 같습니다.

## 4. 결제 보안 3대 핵심

PG 결제 검증 단계에서 반드시 지켜야 하는 세 가지입니다.

첫째, **서버 검증**. 실제로 결제가 됐는지 서버가 PG에 직접 조회해 결제 상태(예: 완료)를 확인합니다.

둘째, **위변조 방지**. 충전·적립 금액은 프론트가 보낸 값이 아니라, PG가 알려준 실제 결제 금액을 사용합니다.

셋째, **중복 결제 방지**. 같은 결제 건이 두 번 처리되지 않도록 고유 식별자에 유니크 제약을 걸고, 이미 처리됐는지 먼저 확인합니다.

## 5. 주문(결제 건) 식별자

각 결제를 구분하는 고유 번호입니다. 포트원 V2에서는 "paymentId"라고 부릅니다. 이 번호는 프론트가 아니라 서버가 발급하는 것이 안전합니다. 서버가 발급해야 유일성을 보장해 중복 결제를 막을 수 있고, 우리 시스템이 그 결제를 책임지고 추적할 수 있기 때문입니다.

비유: 은행 창구에서 손님이 거래번호를 직접 적는 게 아니라, 은행 시스템이 자동으로 일련번호를 부여하는 것과 같습니다.

## 6. 결제 키의 종류와 공개·비밀 구분 (포트원 V2)

포트원 V2에서 쓰는 값은 성격이 다릅니다.

"storeId"(상점 ID)는 어느 상점의 결제인지 식별하는 값으로, 프론트(결제창 호출)에 쓰이는 공개값입니다.

"channelKey"(채널 키)는 어떤 PG·결제수단(예: 카카오페이)으로 결제할지 결정하는 값으로, 역시 프론트에 쓰이는 공개값입니다.

"API Secret"은 서버가 결제를 조회·검증할 때 쓰는 비밀값으로, 절대 외부에 노출하면 안 됩니다.

비유: storeId와 channelKey는 계좌번호(알려줘도 되는 공개값)에, API Secret은 통장 비밀번호(절대 노출 금지)에 해당합니다.

확인 필요: 포트원 V1에서는 가맹점 식별코드와 REST API Key/Secret이라는 다른 이름의 값을 썼습니다. V1과 V2는 값 이름과 발급 위치가 다르므로, 콘솔에서 반드시 V2 항목의 값을 사용해야 합니다.

## 7. 클라이언트 SDK와 서버 REST API의 역할 분담

브라우저 SDK(결제창 SDK)는 사용자에게 결제창을 띄우고 결제를 진행시키는 역할입니다.

서버 REST API는 결제가 끝난 뒤 그 결과를 검증·조회하고, 비밀키를 사용해 PG와 통신하는 역할입니다.

핵심은, 비밀키가 필요한 검증은 반드시 서버에서 한다는 점입니다. 비밀키를 브라우저에 두면 노출되기 때문입니다.

## 8. 미리 갖춰야 할 기술 기반

프론트엔드 쪽에서는 비동기 통신(fetch를 이용한 AJAX), JSON 형식, 그리고 "async/await"(또는 Promise)로 순차 처리하는 방법을 알고 있어야 합니다.

서버(스프링) 쪽에서는 REST API 설계, JSON 요청·응답 처리("@RequestBody", "@ResponseBody"), 로그인 사용자 식별(세션), 외부 API 호출("RestTemplate"), 그리고 여러 작업을 하나로 묶는 트랜잭션("@Transactional")을 알고 있어야 합니다.

데이터베이스 쪽에서는 결제 내역을 저장할 엔티티 설계와, 중복을 막기 위한 유니크 제약을 이해하고 있어야 합니다.

## 9. (심화) 웹훅 — 더 견고한 완료 처리

확인 필요(심화 주제): PG는 결제 결과를 우리 서버로 직접 통지하는 웹훅(webhook)도 제공합니다. 사용자가 결제 직후 창을 닫아 검증 요청이 누락되는 경우까지 대비하려면 웹훅을 함께 쓰는 것이 더 안전합니다. 입문 단계에서는 3단계(준비 → 결제창 → 검증) 흐름을 먼저 이해하고, 웹훅은 그 이후 단계로 다루는 것을 권장합니다.

---

## 한 줄 요약

결제 API의 본질은 "외부(PG)에서 일어난 결제를, 믿을 수 없는 프론트의 말 대신 서버가 직접 확인해서, 위변조와 중복 없이 처리하는 것"이다. 그래서 준비 → 결제창 → 서버 검증의 3단계로 설계한다.
